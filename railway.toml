[build]
  builder = "dockerfile"

[deploy]
  startCommand = "curl -fsSL https://openclaw.ai/install.sh | bash"
  healthcheckPath = "/setup/healthz"
  healthcheckTimeout = 300
  restartPolicyType = "on_failure"

[variables]
  PORT = "8080"    
    
